/**
 * Created by sudiptomanna on 6/14/17.
 */
(function(){
    'use strict';
    console.log(Math.PI);
    var valuePI = Math.PI;
    // Some simple math operator
    console.log(Math.ceil(valuePI));
    console.log(Math.floor(valuePI));
    console.log(Math.round(valuePI));
    console.log(Math.pow(valuePI,2));
    console.log(Math.sqrt(valuePI));
    console.log(Math.random());
}());